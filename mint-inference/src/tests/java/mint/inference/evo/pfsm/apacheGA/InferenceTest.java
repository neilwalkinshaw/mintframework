package mint.inference.evo.pfsm.apacheGA;

/**
 * Created by neilwalkinshaw on 19/05/2016.
 */
public class InferenceTest {

    /*@Test
    public void testInference(){
        BasicConfigurator.configure();
        Configuration configuration = Configuration.getInstance();
        configuration.PREFIX_CLOSED = true;
        configuration.K = 0;
        TraceSet posSet = null;
            try {
                posSet = SpiceTraceReader.readTraceFile("/Users/neilwalkinshaw/Dropbox/tmp/0.spice.train.txt");

            }
            catch(Exception e){
                e.printStackTrace();
            }
            //posSet.getPos().removeAll(posSet.getPos().subList(0,17000));
            ApacheGP gp = new ApacheGP(new GPConfiguration(100,0.9,0.2,6,10),200,posSet, 1000);

            PayloadMachine result = (PayloadMachine)gp.evolve();
           // System.out.println(DotGraphWithLabels.summaryDotGraph(result));


    }*/
}
